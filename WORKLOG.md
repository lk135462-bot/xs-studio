# WORKLOG — XS 工坊 XS Studio

## 2026-08-21（第四輪）寫新手說明時，發現真實下載情境本來會失敗

要寫給電腦新手的操作步驟，先去實測「新手最容易踩的坑」實際會發生什麼——
結果發現上一輪宣告解決的 SAC 問題，在**真實下載情境下並沒有解決**。

### 被漏掉的一環：Mark of the Web

上一輪只驗了「本機建置出來的包」。但使用者拿到的是**從網路下載的 zip**，
Windows 會蓋上 Mark of the Web，而且 **Windows 檔案總管解壓縮時會把這個標記
傳給每一個解出來的檔案**（實測確認）。

實測四種啟動方式（皆帶 MOTW、SAC 強制模式）：

| 啟動方式 | 結果 |
|---|---|
| `.bat` | **封鎖** — 應用程式控制原則已封鎖此檔案 |
| `.cmd` | **封鎖** |
| `.lnk` | **封鎖** |
| 已簽章的 `python.exe` | **正常執行** |

也就是說：只有「已簽章的執行檔」能扛住 MOTW。而先解除封鎖再解壓縮確實可行
（實測），但那要求新手知道「右鍵→內容→解除封鎖」——不能當成主要路徑。

### 解法：把啟動點做成那支已簽章的 exe 本身

- `XS 工坊.exe` ＝ `python.exe` 的副本。**改名不影響 Authenticode 簽章**（實測 Valid）。
- 同名的 `XS 工坊._pth` 只有這支 exe 會讀，內容開 `import site`。
- `sitecustomize.py`（Python 官方機制，site 啟動時自動載入）偵測到
  「argv == [''] 且執行檔名是啟動器」就去跑 `app.py`。

過程中修掉三個陷阱：
1. `._pth` **只吃 `import site`**，其他 `import X` 會報 unsupported——改走 sitecustomize。
2. 在 site 初始化階段 `raise SystemExit` 會變成
   `Fatal Python error: init_import_site`——改用 `os._exit()`。
3. 判斷條件不能用 `len(sys.argv) <= 1`：`python -c "..."` 時 argv 是 `['-c']`，
   會被誤攔。改成 `sys.argv == ['']`。

Python 執行期因此攤平到資料夾根目錄（那支 exe 必須與 `python311.dll` 同層）。

### 副作用與再修

攤平後資料夾有 **47 個可見項目**，對新手是災難。原本想在打包時設隱藏屬性，
但**zip 格式不保留 Windows 隱藏屬性**（實測解出來還是 47 項）。
改由 `app.py` 的 `_tidy_folder()` 在首次啟動時整理 →
**使用者現在只看到 5 項**：啟動器、knowledge、使用說明、README、備援啟動。

「建立桌面捷徑.bat」同樣會被 MOTW 擋，改成介面左下角的【放到桌面】按鈕
（`/api/shortcut`），由已經在跑的程式自己建捷徑，不受該政策管轄。

### 端到端實測（真實的出貨壓縮檔）

把實際要發的 125 MB zip 蓋上「從 Google Drive 下載」的標記 → 用檔案總管的方式
解壓縮 → 雙擊 `XS 工坊.exe`：

- 啟動器 MOTW = True、簽章 = Valid
- **伺服器回應 HTTP 200**，知識庫 17 檔 / 2764 筆
- 首次啟動自動整理，資料夾剩 5 個可見項目

### 再往前追一步：下載那一關

寫第 1 步時想到「他點下載會看到什麼」，查證後發現
**Google Drive 只掃 100 MB 以下的檔案**，超過就對下載者顯示
「Google 無法掃描這個檔案是否含有病毒」。我們的包 125 MB，一定會撞上。
那句話對非技術使用者是勸退級的。

拆解體積：內附的 Claude Code 就佔 315 MB（整包 380 MB）。
因此多做一個 **精簡版 27 MB**（`build.py` 自動連帶產出）——
下載乾淨無警告，代價是走 Claude 訂閱那條路的人要自己裝一次 Claude Code；
走 API Key／本地模型的人兩版完全一樣。

### 產出

- `使用說明.txt`（BOM ＋ CRLF，記事本開不會亂碼也不會擠成一行），
  11 個步驟從「下載」寫到「編譯失敗怎麼辦」，隨三個安裝包一起出。
- 網頁版上手指南（Artifact），可直接發連結給人看。
- 三種安裝包：免安裝版 125 MB／精簡版 27 MB／輕量版 0.4 MB。

### 寫給新手時發現的通則

**寫操作說明要從「他還沒拿到檔案」開始想，不是從「他打開程式」開始。**
這一輪抓到的兩個真問題（MOTW 封鎖、Drive 病毒掃描警告）都在我原本的步驟 1 之前，
是為了寫說明去實測「新手會踩什麼」才浮出來的。
每一步都補上「你應該看到什麼」，是因為新手最缺的不是操作指令，而是「怎麼知道我做對了」。

---

## 2026-08-21（第三輪）走一遍完整操作情境，抓到 .xs 換行問題

用 Playwright 驅動免安裝版，真的點、真的呼叫 AI，模擬「老王第一次打開」的九個步驟。
全程零前端錯誤，九步都通。逐步實測數字見下方「操作情境實測」。

**抓到一個真缺陷**：「存成 .xs」下載的檔案是 **LF-only（58 行）**，
而「複製」那條路徑因為 Windows 剪貼簿會自動正規化，拿到的是 CRLF。
兩條路徑給出不同格式，且檔案那條在只吃 CRLF 的編輯器上會擠成一長行。
已修 `downloadText()`，並實測驗證（61 CRLF / 61 換行、BOM 在、內容一字不差）。
**這跟先前 `.bat` 那個是同一類問題——Windows 上的文字檔換行要當成一等公民處理。**

**兩個一開始誤判為問題、實際是正確行為的**（先分類再動手）：
- 「剪貼簿內容與畫面腳本不一致」：差的 11 個字元全是 `
`，是 Windows 剪貼簿的
  正規化，內容一字未少——而且貼進 XQ 反而是對的。用一段已知腳本逐字元比對確認。
- 「追加條件時沒有 [OLD]/[NEW] 對照」：那一輪是**新增**條件、沒有東西被取代，
  本來就不會有 OLD。它用了 `// [NEW]` 行內標記＋「本輪變更清單」表格，
  還補充了「為何月線用 `close > _ma` 而非 `crosses over`」的設計理由。
  是我的測試斷言寫太嚴，不是產品的問題。

### 操作情境實測（免安裝版，2026-08-21）

| 步驟 | 結果 |
|---|---|
| 1 首次開啟 | 連線嚮導自動跳出（10.0 s）；偵測到「已登入 …（max 訂閱）」；關閉鈕隱藏，不給跳過 |
| 2 測試連線 | 3.7 s，回「連線成功，AI 回覆：『可用』」 |
| 3 進主畫面 | AI 連線＝Claude Code 訂閱；知識庫＝17 份文件 · 2,764 條索引 |
| 4 點起手式 | 一鍵帶入輸入框，不必自己想第一句 |
| 5 送出→寫完 | 28.3 s；引用 8 筆知識庫；四大區塊／全小寫／底線前綴三項規範全過 |
| 6 按複製 | 按鈕變「已複製」，剪貼簿 1,456 字元（含 CRLF 正規化） |
| 7 追加條件 | 23.4 s；標出改了哪些、完整可貼的腳本仍給 |
| 8 存成 .xs | 檔名自動取自腳本名稱；BOM ＋ CRLF ＋ 內容一字不差 |
| 9 重開頁面 | 嚮導不再跳出，連線設定已記住 |

---

## 2026-08-21（第二輪）上網查證後，把 SAC 封鎖這關解掉

上一輪卡在「未簽章 EXE 被 Smart App Control 直接封鎖」，只能退而求其次發輕量版。
這一輪查證＋實測後找到正解，主力分發形態改為**免安裝版**。

### 關鍵領悟

SAC 管的是**可執行映像**，不是程式語言。所以問題從來不是「用不用 Python」，
而是**整包裡有沒有一支沒人擔保的 .exe**。PyInstaller 一定會產生一支自己的
bootloader exe——那就是被擋的東西。

改用 Python 官方 **embeddable 版**後，整包只剩別人已簽好的執行檔：

| 執行檔 | 簽署者 | 實測 |
|---|---|---|
| `python\python.exe` | Python Software Foundation（DigiCert） | Valid |
| `claude.exe`（SDK 內附） | Anthropic, PBC（EV） | Valid |

我們自己只出 `.py` 與 `.bat`，都不是可執行映像。

### 驗收（全部在這台 SAC 強制模式的機器上實跑）

- 免安裝版**啟動成功**——這正是上一輪做不到的事。首頁 200、知識庫 17 檔 2764 筆掛載。
- 強制走內附 `claude.exe`（模擬從沒裝過 Claude Code 的使用者）→ 實際回應正確。
- 完整對話：首字 3.9 s、全長 29 s，腳本結構與規範正確。
- 打包時逐支 `.exe` 查 Authenticode → 零未簽章執行檔。
- 體積：資料夾 380 MB、壓縮檔 **125 MB**（PyInstaller 版是 324 MB 資料夾）。

### 順手抓到一個會讓所有使用者壞掉的 bug

打包版第一次跑對話直接失敗：
`Refusing to execute batch script 'claude.cmd'`。

根因是**開發機與使用者機器的 SDK 版本不同**——
系統裝的是 0.2.93（還吃 `.cmd`），打包時 pip 拉到 0.2.142（拒絕 `.bat`／`.cmd`，
理由是 cmd.exe 有參數注入風險）。而 `_find_claude_cli()` 當時照直覺
「使用者自己裝的優先」，npm 裝的正好就是 `claude.cmd`。

於是在開發機上一切正常、打包出去每一次對話都失敗——**只在打包後實跑才會發現**。
改成「原生 exe → 內附 exe → 最後才是 `.cmd`」，並加 `tests/test_cli_detection.py`
（5 項）鎖住這個順序。

### 打包腳本本身也修了兩處

- **簽章稽核撈不到檔案**：PowerShell 的 stdout 走主控台編碼（本機 cp950），
  中文資料夾名一往返就變亂碼，`Path` 再也對不上實體檔——清理程式因此一支都沒刪掉，
  **卻不會報錯**。改成路徑用 UTF-8 暫存檔進去、只帶「第幾支＋狀態」回來。
- **清掉用不到的未簽章執行檔**：pip 產生的 `flask.exe`／`uvicorn.exe` 殼、
  pywin32 的 `Pythonwin.exe`／`pythonservice.exe`，我們一支都不跑，
  留著只是白白給 SAC 攔截的理由。

### 三種形態現況

| 形態 | 會被擋嗎 | 體積 | 狀態 |
|---|---|---|---|
| 免安裝版（主力） | **不會**（已實測） | 125 MB zip | 可以發 |
| 輕量版 | 不會 | 0.4 MB zip | 可以發，適合工程背景 |
| PyInstaller 版 | 會 | 324 MB | 需簽章憑證，`build.py exe` 保留但不列入 `all` |

補查到的第三條路：若專案改為開源，**SignPath Foundation 提供免費 OV 等級簽章**
給符合資格的開源專案。

---

## 2026-08-21 立案並完成 v0.1.0

### 做了什麼

從 `Downloads/xs-skill.zip` 開案，做出一個獨立的 XS 撰寫前台。

- **解壓縮轉碼**：zip 內檔名是 cp950，直接解會變亂碼（`XSAI��Ʈw`）。
  解壓時逐檔 `encode('cp437').decode('cp950')` 轉回「XSAI資料庫」。
- **知識庫檢索層**（`xs/knowledge.py`）：17 檔切成 2764 筆原子段落，
  建索引 23 ms、單次檢索 7–24 ms。分常駐層（23K 字）與檢索層（限 8 筆／45K 字）。
- **`[LOOKUP]` 二段檢索**：補回 Claude Code 才有的 grep 能力，讓四種後端品質一致。
- **四後端 ＋ 連線嚮導**：LLM 層改編自萬界 `omni/llm.py`，Windows 地雷沿用其解法。
- **介面**：深色交易工具調性，色票沿用 XQ 深色 fintech 家族（xq-photocard）。
- **兩種分發**：綠色免安裝 EXE（324 MB）＋ 輕量 zip（0.4 MB）。

### 驗收紀錄

實跑正式路徑，不用簡化腳本代替：

- 端到端對話（Claude Code SDK）：首字 5.3 s、全長 29.6 s、2036 字。
  產出的選股腳本符合四大區塊、`getfield` 補 `default:=0`、跨頻率取前期直接對函數
  加中括號——兩條最容易寫錯的鐵則都守住了，並附來源標註。
- `tests/test_chat_flow.py` 7 項全過；`tests/test_oai_stream.py` 4 項全過。
- 迭代模式（貼既有腳本要求改停損）：32.5 s，`[OLD]`/`[NEW]` 對照正確、
  只動停損那幾行、原有進場邏輯與 `setposition` 保留，未整支重寫。
- 啟動器實跑：`啟動 XS 工坊.bat` 起得來、中文顯示正常、首頁 HTTP 200。
- Playwright 桌面／手機截圖驗收，console 0 錯誤。
- 打包產物：模擬 frozen 狀態實跑 dist 內容，頁面 200、靜態檔 200、知識庫讀到
  EXE 旁那份。

### 過程中修掉的缺陷

視覺驗收（截圖）抓到四個，都不是「有點怪」而是真缺陷：

1. **程式碼區塊每一行都被畫成小方框** — `.msg-body code` 的行內樣式漏進了
   `pre > code`。補 `.code-block pre code{background:none;border:0;padding:0}`。
2. **引言列的 `>` 原樣顯示、沒有樣式** — markdown 解析在 `escapeHtml` 之後才跑，
   此時 `>` 已經是 `&gt;`，`^>` 的比對永遠不成立。改比對 `^&gt;`。
3. **窄螢幕輸入框被擠出畫面、內容被蓋住** — `.main{height:100vh}` 在
   `grid-template-rows:auto 1fr` 的列裡會超出列高。改 `height:100%` ＋ `min-height:0`。
4. **窄螢幕頂列硬塞整個側欄，擠成一團還被裁切** — 改成只留品牌／開新對話／連線狀態。

**第五個、也是最嚴重的一個**：`啟動 XS 工坊.bat` 是 LF 換行。
Windows 的 cmd.exe 解析 `.bat` 需要 CRLF，只有 LF 會讓它把第一行讀壞
（實測第一行變成 `'???echo'`），整支檔案跑不起來——**每一個使用者雙擊都會失敗**。
若不是實際去跑那支 bat 而只是「看起來沒問題」，這個會直接帶進發行版。
已修：bat 改寫成 CRLF、`build.py` 產生的 bat 走 `_write_bat()`、
加 `.gitattributes` 鎖 `*.bat text eol=crlf` 防止 git 正規化回 LF。

另外修掉：Flask 3 已移除 `JSON_AS_ASCII`（改用 `app.json.ensure_ascii`）、
favicon 404、EXE 主控台輸出在 cp950 下是亂碼（啟動時 `SetConsoleOutputCP(65001)`）。

### 兩個影響決策的發現

**1. 未簽章 EXE 被 Smart App Control 直接封鎖（WinError 4551）。**
本機 SAC 為強制模式，連續三次重試皆被擋。打包產物本身沒問題（已另行驗證），
被擋的是 Windows 執行政策。因此保留輕量 zip 作為今天就能發的路線，
而不是只做 EXE。查證後補充：台灣公司不符合 Azure Artifact Signing 資格，
要簽章得走傳統 CA 的 OV 憑證；且 EV 自 2024-03 起已無 SmartScreen 即時信任特權，
不必為此多付錢。詳見 DISTRIBUTION.md。

**2. `claude-agent-sdk` 內含完整 Claude Code 執行檔（230 MB）。**
原本只當作體積負擔，實際上是推廣的關鍵資產——有 Claude 訂閱但沒裝過 Claude Code
的人，不必先裝 Node、不必碰終端機，登入一次就能用。
已把 `_find_claude_cli()` 改成「先用使用者自己裝的，沒有才用內附那份」，
並用 `claude auth status`（回 JSON）做真實登入狀態偵測、`claude auth login` 做一鍵登入。

### 未完成 / 未驗證

- ~~**EXE 雙擊啟動未能驗收**~~ — 第二輪已用免安裝版繞過並實測通過（見上）。
- **Anthropic API 與 OpenRouter 未用真實金鑰實測** — 手上沒有金鑰。
  兩者共用的串流解析與錯誤處理已用假端點驗過，但真實服務的回應細節未對照。
- **本地模型未接真實 Ollama 實測** — 本機沒跑推論服務，偵測與串流以假端點驗證。

### 下一步候選

1. 取得 OV 簽章憑證並把簽章接進 `build.py`（對外推廣的前置）
2. 範例腳本畫廊——素材已在 `knowledge/`，只差一個列表頁
3. 對話存檔（目前重開就沒了）
4. 腳本說明區帶來源註記，讓 `.xs` 檔自己帶傳播
